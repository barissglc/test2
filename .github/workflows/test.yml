- name: Test bol function
  run: |
    output=$(printf "10\n2\n" | ./fonksiyon)
    if [[ $(echo "$output" | awk '{print $4}') == "5.00" ]]; then
      echo "Bol function test passed"
    else
      echo "Bol function test failed"
      echo "$output"
      exit 1
    fi
